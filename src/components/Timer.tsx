import { TimerLabelStyled, TimerStyled } from "../styles/TimerStyled";

const Timer = () => {
  return (
    <TimerStyled>
      <TimerLabelStyled id="timer-label">Session</TimerLabelStyled>
    </TimerStyled>
  );
};

export default Timer;
